export type UserProfileTypes = {
  firstName: string;
  lastName: string;
  gender: string;
  country: string;
  state: string;
  address: string;

};

export type ChangePasswordTypes = {
  oldPassword: string;
  newPassword: string;
  confirmPassword: string;
};
